/*
 * 실제 공고문 원문 회귀 테스트 (Q01~Q07) - 봉선동 르 오네뜨 2차(광주광역시 남구).
 * 사용자가 "PDF 인식은 되는데 타입별 분양가/확장비 등으로 전환이 안 된다"고 제보하며 새로
 * 업로드한 5개 실사례 문서 중 하나. 원인을 추적해 네 가지 실패 패턴을 발견했다:
 *   - 공급면적표의 약식표기가 표 셀 줄바꿈으로 "130\nA1"처럼 숫자 접두부와 문자 접미부가
 *     서로 다른 토큰으로 쪼개져(PDF 추출 시 사이에 공백만 남음) 코드 자체를 인식하지 못하고
 *     숫자만 남는 최후수단 규칙으로 잘못 대체되며, 130A1/130A2 두 타입이 "130" 하나로 뭉개짐
 *     (reglueSplitShortCodes로 "숫자 토큰+문자 접미부 토큰"을 근처 반복되는 실수 면적값으로
 *     검증한 뒤 재결합).
 *   - 발코니/옵션 섹션 소제목이 괄호 대신 대괄호로 "[단위 : 원, VAT 포함]"이라 쓰여
 *     restrictToFirstUnitMarkerSubsection이 인식하지 못했고, 그 결과 뒤이은 무관한
 *     "개별 선택옵션"(품목별 카탈로그, 코드 없음) 표의 값이 코드에 잘못 붙었다(괄호/대괄호 모두
 *     인정하도록 확장).
 *   - 헤더 키워드 "구분"의 두 글자 사이에 공백이 낀 "구 분"도 카탈로그형 표 판별에서 인식하도록
 *     stripCatalogTables/extractColumnAlignedCandidates를 넓혔다(같은 문서의 "계 약 금"/
 *     "중 도 금"처럼 PDF 추출 시 글자 사이 공백이 끼는 문제가 "구분"에도 있었음).
 *   - "130A1"/"130A2"는 공고문 원문에 "무작위 배정"이라고 명시된, 같은 주택형(130.0926)에
 *     속한 두 타입이라 공급금액표에 층별 행이 한 세트만 나오고 두 타입이 이를 공유한다.
 *     scanPriceRowsWithMap이 "코드가 가격 섹션 전체에서 정확히 한 번만 등장하는가"를 근거로
 *     삼아, 진짜 공유(코드가 이 한 지점에서만 유일하게 언급됨)와 단순 안내문(코드가 나중에
 *     자기 이름의 데이터 블록을 또 가짐 - 실사례: 북수원이목지구 리체Ⅰ의 "84B 84C" 매핑 안내)
 *     을 안전하게 구분한다.
 */
var AREA_TEXT = [
  '■ 공급대상 [단위 : ㎡, 세대]',
  '주택관리번호 모델 주택형',
  '(전용면적기준) 약식표기',
  '주택공급면적(㎡) 기타공용면적',
  '(지하주차장 등) 계약면적 세대별',
  '대지지분',
  '타입별',
  '세대수',
  '총공급',
  '세대수',
  '특별공급 세대수 일반공급',
  '세대수',
  '최하층',
  '우선배정',
  '세대수주거전용면적 주거공용면적 소계 다자녀가구 노부모부양 계',
  '2025000238',
  '01 130.0926',
  '130 A1 130.0926 26.1192 156.2118 117.8407 274.0525 71.3975 28',
  '54 5 2 7 47 4',
  '130 A2 130.0926 26.1192 156.2118 117.8407 274.0525 71.3975 26',
  '02 142.7005 142 PH 142.7005 27.9251 170.6256 129.2613 299.8869 78.3170 2 2 - - - 2 -',
  '합계 56 56 5 2 7 49 4'
].join('\n');

var PRICE_TEXT = [
  '■ 공급금액 및 납부일정(발코니 확장금액 별도) [단위 : 세대, 원]',
  '주택형',
  '타입',
  '(약식',
  '표기)',
  '동 층',
  '해당',
  '세대',
  '수',
  '공급금액 계 약 금(10%) 중 도 금(60%) 잔 금(30%)',
  '1차(5%) 2차(5%) 1회(10%) 2회(10%) 3회(10%) 4회(10%) 5회(10%) 6회(10%)',
  '대지비 건축비 부가세 합계 계약시 계약 후',
  '30일 이내 2025.11.17 2026.03.16 2026.07.15 2026.11.16 2027.03.15 2027.07.15 입주지정일',
  '130. 926 130A1',
  '130A2 101',
  '2 4 446,097,110 627,184,445 62,718,445 1,136,000,000 56,800,000 56,800,000 113,600,000 113,600,000 113,600,000 113,600,000 113,600,000 113,600,000 340,800,000',
  '3 4 446,097,110 629,911,718 62,991,172 1,139,000,000 56,950,000 56,950,000 113,900,000 113,900,000 113,900,000 113,900,000 113,900,000 113,900,000 341,700,000',
  '4 4 446,097,110 632,638,991 63,263,899 1,142,000,000 57,100,000 57,100,000 114,200,000 114,200,000 114,200,000 114,200,000 114,200,000 114,200,000 342,600,000',
  '5 4 446,097,110 665,366,264 66,536,626 1,178,000,000 58,900,000 58,900,000 117,800,000 117,800,000 117,800,000 117,800,000 117,800,000 117,800,000 353,400,000',
  '6~10 20 446,097,110 697,184,445 69,718,445 1,213,000,000 60,650,000 60,650,000 121,300,000 121,300,000 121,300,000 121,300,000 121,300,000 121,300,000 363,900,000',
  '11~15 18 446,097,110 714,457,173 71,445,717 1,232,000,000 61,600,000 61,600,000 123,200,000 123,200,000 123,200,000 123,200,000 123,200,000 123,200,000 369,600,000',
  '142.7005 142PH 101 15 2 489,330,528 873,335,884 87,333,588 1,450,000,000 72,500,000 72,500,000 145,000,000 145,000,000 145,000,000 145,000,000 145,000,000 145,000,000 435,000,000'
].join('\n');

var BALCONY_TEXT = [
  '■ 발코니 확장 공사 계약',
  '① 발코니 확장 공사비 [단위 : 원, VAT 포함]',
  '약식표기 발코니 확장 공사비 계약금(10%) 잔금(90%) 비고 계약시 입주지정일',
  '130A1 65,000,000 6,500,000 58,500,000',
  '130A2 65,000,000 6,500,000 58,500,000',
  '142PH 80,000,000 8,000,000 72,000,000'
].join('\n');

var OPTION_TEXT = [
  '■ 추가선택품목(유상옵션)',
  '① 천장형 시스템에어컨 [단위 : 원, VAT 포함]',
  '주택형',
  '(약식표기) 제조사명 설치위치 및 수량 공급금액 계약금 10% 잔금 90% 비고',
  '계약시 입주시',
  '130A1,130A2 삼성 [ 5개소 ] 거실+주방+침실1(안방)+침실2+침실3 12,700,000 1,270,000 11,430,000',
  '142PH [ 6개소 ] 거실+주방+침실1(안방)+침실2+침실3+침실4 18,800,000 1,880,000 16,920,000',
  '② 추가 선택 품목',
  '1. 르 오네뜨 시그니처 패키지 [단위 : 원, VAT 포함]',
  '주택형',
  '(약식표기) 구성 공급금액 계약금(10%) 잔금(90%) 비고',
  '계약시 입주시',
  '130 A1, 130A2 천장형 에어컨[5개소] / 벽체 스타일링 50,000,000 5,000,000 45,000,000',
  '142 PH 천장형 에어컨[6개소] / 벽체 스타일링 59,500,000 5,950,000 53,550,000',
  '2. 개별 선택옵션 [단위 : 원, VAT 포함]',
  '구분 제품명 공급금액 계약금(10%) 잔금(90%) 비고',
  '계약시 입주시',
  '침실2 빌트인 붙박이장 2,800,000 280,000 2,520,000'
].join('\n');

module.exports = [
  {
    id: 'Q01',
    desc: '공급면적표: "130\\nA1"/"130\\nA2"처럼 줄바꿈으로 쪼개진 약식표기도 130A1/130A2 두 타입으로 정확히 분리 인식(세대수 28/26, 합계 54)',
    run: function (p) {
      var area = p.parseAreaSection(AREA_TEXT);
      var a1 = area.find(function (x) { return x.code === '130A1'; });
      var a2 = area.find(function (x) { return x.code === '130A2'; });
      var ph = area.find(function (x) { return x.code === '142PH'; });
      return area.length === 3
        && a1 && a1.supply_units === 28 && a1.exclusive_area === 130.0926
        && a2 && a2.supply_units === 26
        && ph && ph.supply_units === 2 && ph.exclusive_area === 142.7005;
    }
  },
  {
    id: 'Q02',
    desc: '공급금액표: 코드 인식이 정상화된 뒤에는 회차별 날짜(6회, 2025.11.17~2027.07.15)도 함께 정확히 인식됨(이전엔 코드가 깨져 0/6으로 버려짐)',
    run: function (p) {
      var codes = ['130A1', '130A2', '142PH'];
      var price = p.parsePriceSection(PRICE_TEXT, codes);
      var dates = price.midDates;
      return dates.length === 6 && dates.every(Boolean)
        && dates[0].getFullYear() === 2025 && dates[0].getMonth() === 10 && dates[0].getDate() === 17
        && dates[5].getFullYear() === 2027 && dates[5].getMonth() === 6 && dates[5].getDate() === 15;
    }
  },
  {
    id: 'Q03',
    desc: '공급금액표: 142PH 타입 분양가(1,450,000,000원)/세대수(2)가 정확히 인식됨',
    run: function (p) {
      var codes = ['130A1', '130A2', '142PH'];
      var price = p.parsePriceSection(PRICE_TEXT, codes);
      var row = price.priceRows.find(function (x) { return x.code === '142PH'; });
      return !!row && row.price === 1450000000 && row.units === 2;
    }
  },
  {
    id: 'Q07',
    desc: '공급금액표: "130A1\\n130A2"는 무작위 배정으로 가격표를 통째로 공유하므로(공고문 원문에 명시됨) 두 타입 모두 같은 6개 층별 행 전체(세대수 합계 54)를 그대로 반영 - 코드가 가격 섹션 전체에서 정확히 한 번만 등장할 때만 공유로 인정(H03/O04처럼 안내문에 코드가 나란히 언급되거나 별칭이 우연히 겹치는 경우와는 안전하게 구분됨)',
    run: function (p) {
      var codes = ['130A1', '130A2', '142PH'];
      var price = p.parsePriceSection(PRICE_TEXT, codes);
      var a1 = price.priceRows.filter(function (x) { return x.code === '130A1'; });
      var a2 = price.priceRows.filter(function (x) { return x.code === '130A2'; });
      var sum = function (rows) { return rows.reduce(function (s, r) { return s + r.units; }, 0); };
      return a1.length === 6 && a2.length === 6 && sum(a1) === 54 && sum(a2) === 54
        && a1[0].price === a2[0].price;
    }
  },
  {
    id: 'Q04',
    desc: '발코니 확장비: 대괄호형 "[단위 : 원, VAT 포함]" 소제목도 인식되어 130A1/130A2(65,000,000)/142PH(80,000,000) 정확히 반영(이전엔 괄호만 인식해 뒤 무관한 표까지 섞임)',
    run: function (p) {
      var codes = ['130A1', '130A2', '142PH'];
      var balc = p.parseBalconySection(BALCONY_TEXT, codes);
      return balc['130A1'] === 65000000 && balc['130A2'] === 65000000 && balc['142PH'] === 80000000;
    }
  },
  {
    id: 'Q05',
    desc: '추가선택품목(옵션): 대괄호형 소제목 인식으로 두 번째 소제목("르 오네뜨 시그니처 패키지")부터는 잘라내고, 첫 소제목(천장형 시스템에어컨)의 130A1/130A2(12,700,000 공유)/142PH(18,800,000)만 정확히 반영',
    run: function (p) {
      var codes = ['130A1', '130A2', '142PH'];
      var opt = p.parseOptionSection(OPTION_TEXT, codes);
      return opt['130A1'] === 12700000 && opt['130A2'] === 12700000 && opt['142PH'] === 18800000;
    }
  },
  {
    id: 'Q06',
    desc: '추가선택품목(옵션): 대괄호 인식이 없었다면 뒤에 나오는 "개별 선택옵션"(코드 없는 카탈로그, 예: 침실2 붙박이장 2,800,000)의 무관한 값이 130A2에 잘못 붙었을 것 - 지금은 붙지 않음',
    run: function (p) {
      var codes = ['130A1', '130A2', '142PH'];
      var opt = p.parseOptionSection(OPTION_TEXT, codes);
      return opt['130A2'] !== 2800000;
    }
  }
];
